{
  "name": "saweria-pg",
  "version": "1.0.0",
  "description": "Saweria Payment Gateway as a Service — Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute saweria-pg-db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute saweria-pg-db --remote --file=./schema.sql",
    "types": "wrangler types"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260420.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.83.0"
  }
}
